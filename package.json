{
	"name": "crypto-core-plex",
	"version": "1.0.0",
	"author": "MaKarastY",
	"scripts": {
		"build": "tsc --build tsconfig.json --force --verbose && echo '✅' || echo '❌'",
		"start-node": "node --optimize-for-size dist/src/main.js",
		"build-and-run": "npm run build && npm run start-node"
	},
	"devDependencies": {
		"@types/node": "^22.13.14",
		"typescript": "^5.8.2"
	},
	"dependencies": {
		"boxen": "^8.0.1",
		"colorette": "^2.0.20"
	}
}